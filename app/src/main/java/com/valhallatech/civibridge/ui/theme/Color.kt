package com.valhallatech.civibridge.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores base
val OrangePrimary = Color(0xFFFF9800)      // Naranja primario
val OrangeDark = Color(0xFFE65100)         // Naranja oscuro para acentos
val OrangeLight = Color(0xFFFFB74D)        // Naranja claro

// Colores de modo claro
val BackgroundLight = Color(0xFFF8F8F8)    // Fondo principal (casi blanco)
val SurfaceLight = Color(0xFFF5F5F5)       // Superficie de tarjetas (blanco puro)
val OnPrimaryLight = Color(0xFFFFFFFF)     // Texto sobre color primario (blanco)
val OnBackgroundLight = Color(0xFF212121)  // Texto sobre fondo (negro suave)
val OnSurfaceLight = Color(0xFF212121)     // Texto sobre superficie (negro suave)
val CardLight = Color(0xFF424242)          // Color para tarjetas en modo claro
val BlackForLightMode = Color(0xFF000000)  // Negro puro para usar en modo claro

// Colores de modo oscuro
val BackgroundDark = Color(0xFF121212)     // Fondo principal (casi negro)
val SurfaceDark = Color(0xFF242424)        // Superficie de tarjetas (gris muy oscuro)
val OnPrimaryDark = Color(0xFF000000)      // Texto sobre color primario (negro)
val OnBackgroundDark = Color(0xFFE0E0E0)   // Texto sobre fondo (blanco suave)
val OnSurfaceDark = Color(0xFFE0E0E0)      // Texto sobre superficie (blanco suave)
val CardDark = Color(0xFFE0E0E0)           // Color para tarjetas en modo oscuro
val BlackForDarktMode = Color(0xFFFFFFFF)  // Negro puro para usar en modo claro

// Colores para estados y retroalimentación
val ErrorColor = Color(0xFFE53935)         // Rojo para errores
val SuccessColor = Color(0xFF4CAF50)       // Verde para éxito
val WarningColor = Color(0xFFFFC107)       // Amarillo para advertencias


